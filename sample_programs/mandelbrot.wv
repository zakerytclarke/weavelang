MAX_ITER = 80

def mandelbrot(c):
    z = 0
    n = 0
    while abs(z) <= 2 and n < MAX_ITER:
        z = z*z + c
        n += 1
    return n


for(i,[0:10]){
 for(j,[0:10]){
  x[j]:=mandelbrot(i,j);
 } 
 print(X);
}